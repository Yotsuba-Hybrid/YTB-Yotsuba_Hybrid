<Project>
  <!--
    SandBoxGame/Directory.Build.props
    Importa el props raíz (que define UseV2 via Configuration).
  -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
</Project>
